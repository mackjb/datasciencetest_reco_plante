name: plantvillage_pre_processing
channels:
  - defaults
  - pytorch
  - conda-forge
dependencies:
  - python=3.9
  - pip
  - numpy
  - pillow
  - pytorch
  - torchvision
  - torchaudio
  # Si vous souhaitez utiliser le GPU, décommentez la ligne suivante et ajustez la version cudatoolkit à votre configuration :
  # - cudatoolkit=11.7
  - pip:
    - segment-anything